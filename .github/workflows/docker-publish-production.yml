name: Build & Deploy Production

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 🧾 Checkout source
      uses: actions/checkout@v4

    - name: 🐳 Docker Login
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: 📦 Get version from package.json
      id: package-version
      run: echo "version=$(cat package.json | jq -r .version)" >> $GITHUB_OUTPUT

    - name: 🛠️ Build & Push Docker Image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: |
          calypso666/eznotif:${{ steps.package-version.outputs.version }}
          calypso666/eznotif:latest

    - name: 🚀 Deploy via SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USER }}
        key: ${{ secrets.SSH_KEY }}
        script: |
          cd ~/own
          docker compose down eznotif || true
          docker compose down eznotif-staging || true
          docker pull calypso666/eznotif:latest
          docker compose up -d eznotif
          exit